# Соответствие тех. части регламенту оплаты

Сверка `specs/payment-regulation.md` (14 шагов) с реализацией.
Обновлено: 2026-07-10, после правок по итогам сверки.

Легенда: ✅ соответствует · 🔧 закрыто правками 2026-07-10 · 📋 спека
`specs/aml-adapter.md` (реализация по фазам) · ⏭ вне контура коннектора.

| Шаг регламента | Статус | Как реализовано / что делать |
|---|---|---|
| Пред. условия: УНК, уведомление о кошельке, маппинг 1С | ✅/⏭ | УНК — `Contract.registration_number` из синхронизации 1С; уведомление о кошельке — организационное |
| 1. Инвойс в 1С → коннектор считывает | ✅ | Синхронизация из 1С (`POST /api/v1/onec/sync`); направление — 1С пушит фоновым заданием (функционально эквивалентно) |
| 1а. Адрес кошелька нерезидента в инвойсе | 🔧 | Поле `Invoice.crypto_address` + автопопадание в справочник адресов контрагента (origin=invoice), сеть определяется по формату адреса — система знает адрес ДО платежа |
| 2. ExpectedPayment (ожидаемый платёж, статусы) | 📋 | Спека AML, фаза 2; допуск ±0.5% уже дефолт матчинга, КВВО 🔧 — поле контракта из 1С |
| 3. AML-проверка ДО отправки (Crystal, шкала 0–30/31–70/71–100) | 📋 | Спека AML, фазы 1–2: AmlAdapter (Crystal + мок), пороги конфигурируемы |
| 4. Решение комплаенс-офицера | 📋 | Спека AML, фаза 3: роль `compliance`, очередь review, решения в аудит |
| 5. Казначей отправляет (экран одобренных) | 📋 | Спека AML, фаза 3: роль `treasurer`; отправка — из внешнего кошелька (read-only принцип сохранён, как и в регламенте) |
| 6. Обнаружение транзакции (hash, сумма, fee, блок) | ✅ | Индексер: статусы, комиссия, курсор, кросс-проверка источников |
| 7. Матчинг (адрес + сумма ±0.5% + окно) | ✅ | Правила 1–3, частичные оплаты (сценарий 3 регламента), запоминание паттерна, ручной разбор |
| 8. Фиксация курса (USDT/USD + USD/RUB ЦБ) | ✅ | Снимок на финальность: цепочка актив→валюта→рубль, источники сохраняются |
| 9. Документ в 1С (УНК, КВВО, tx_hash, «Проведён») | ✅+🔧 | Проекты документов с идемпотентностью; КВВО добавлен в привязки 🔧; «Проведён» — режим авто-проведения (константа расширения); AML-комментарий — после спеки AML |
| 10. Проверка бухгалтером | ⏭ | Сторона 1С (предусмотрено расширением) |
| 11. Уведомление ФНС (XML для ТКС + PDF) | 🔧 | `GET /api/v1/reports/fns-notification/{tx_id}` — HTML (печать/PDF), XML под ТКС, JSON; все поля регламента; блок AML — честный статус not_performed до реализации спеки |
| 12. Подпись ЭЦП и отправка | ⏭ | Бухгалтер, вне коннектора (по регламенту) |
| 13. Иммутабельный аудит-трейс | ✅ (частично) | Аудит-лог + неизменяемость первички (PG-триггеры); полная цепочка invoice→aml→tx→document→notification — после спеки AML |
| 14. Выгрузка в банк | ⏭ | Стандартная выгрузка 1С; КВВО/УНК в документе есть |
| Сценарий 2 (курс отклонился > допуска) | ✅ | Остаток/переплата → ручной разбор |
| Сценарий 4 (сбой API сети) | ✅ | Ретраи цикла, алерты недоступности источников; таймер «30 минут после отправки» — спека AML, фаза 4 |
| Сценарий 5 (пояснения ФНС) | ✅+🔧 | Карточка транзакции + акт + уведомление; полный трейс — после спеки AML |

## Расхождения регламента с тех. частью, требующие решения владельца

1. **Шаг 9: «Документ сохраняется как „Проведён"»** противоречит принципу
   ТЗ «проведение — на стороне бухгалтера» и шагу 10 самого регламента
   (бухгалтер проверяет и подтверждает). Тех. часть поддерживает оба
   режима (константа КоннекторАвтопроведение); рекомендация — «проект»
   до подтверждения, иначе шаг 10 теряет смысл.
2. **Шаг 2: курс из «CoinGecko/Binance»** — в тех. части источник курса
   определяется учётной политикой (привязка/биржевой/CoinGecko);
   Binance-адаптер добавляется одним классом при необходимости.
3. **Форма КНД 1123456 (проект)** — официального формата нет; XML
   уведомления построен нейтрально и будет замаплен при выходе формата
   (принцип обновляемых форм).
